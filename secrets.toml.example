# Copy this file to ".streamlit/secrets.toml" and fill in your own key.
# NEVER commit the real secrets.toml file to GitHub — it is already listed
# in .gitignore.
#
# This key is optional. If it is missing, the chatbot automatically falls
# back to built-in rule-based answers, so the app still works without it.

GEMINI_API_KEY = "your-gemini-api-key-here"
